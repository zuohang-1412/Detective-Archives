# 侦探档案馆上线检查表

更新于 2026-07-22。已勾选项均有本地、真实数据库、隔离恢复库或 GitHub 候选环境证据；未勾选项必须在正式微信、生产服务器或真实运营主体条件下完成，不以开发替身代替。

## P0：不通过不得上线

- [x] `npm ci` 和 `npm run check` 全部通过。
- [x] 分阶段上线审计器已覆盖 31 个非敏感门禁，部署脚本会在构建前强制通过 14 个预部署输入与一致性检查；提审阶段校验运营演练绑定回执。
- [ ] 真实生产环境执行 `npm run release:check` 通过。
- [x] 所有数据库迁移成功，`npm run check:db` 与 `npm run check:db-api` 通过；双镜像连续启动后 109 条图鉴人物关联仍完整。
- [ ] 微信小程序 AppID、合法域名、服务类目、隐私保护指引和用户协议已配置；客户端已完成微信隐私状态查询、平台指引入口和授权同步控件，仍需在公众平台填写并审核真实声明。
- [ ] API 域名 HTTPS 证书有效，HTTP 自动跳转 HTTPS，数据库未暴露公网。
- [ ] 微信真机完成登录、书架、评论、社区动态、举报、个人数据导出/文件分享、注销全链路验证。
- [x] 小程序候选预览/上传入口已固定官方工具版本，强制仓库外私钥/二维码、完整发布门禁、最小子进程环境和带 AppID/提交的成功回执；无真实凭证时失败关闭。
- [ ] 使用真实代码上传密钥和 IP 白名单生成体验版并完成候选上传；上线清单中的 `candidateUploadReceipt` 与公众平台版本一致。
- [x] 后台完成登录、审核、举报、用户处置和作品链接维护验证。
- [x] 未登录用户不能写入；普通用户不能访问后台；待审核内容不会公开。
- [x] 受限账号的普通写入返回 403，但账号状态、个人数据导出、退出和注销仍可用；导出限频、审计且排除令牌/哈希、审核员身份和内部处置备注。
- [ ] 微信文本内容安全接口可用；明确风险内容拒绝，接口故障时内容保持人工待审且审计记录状态。
- [x] 生产日志配置已通过真实 Pino 输出验证，不包含 AppSecret、后台/数据库密码、微信 code、Cookie、完整 Bearer 或会话令牌；上线后仍需抽样复核采集端。
- [x] `ops/deploy-release.sh` 已生成上线前数据库备份，且备份在隔离环境完成恢复验证。
- [x] GitHub 候选环境以两个不同镜像 ID 实际执行 `ops/deploy-release.sh` 和 `ops/rollback-release.sh`，并验证强制备份、`.release-state`、回滚后镜像、数据库与 HTTP。
- [ ] 正式服务器的 `.release-state/previous-image-tag` 对应镜像仍在本机，并已完成一次带生产监控证据的真实回滚演练。
- [x] 已在真实库克隆的隔离库演练最新内容批次的补偿回滚，确认只归档目标内容、停用对应链接并保留用户关系；正式运营人员仍需在生产来源隔离库复演。

## P1：首发前完成

- [ ] `/ready` 外部探测与 `/metrics` 采集已接入，告警接收人明确。
- [ ] 每日数据库备份任务已启用，备份目录已加密并同步到独立存储。
- [ ] CORS 无通配符，登录和社区写接口限流生效。
- [ ] 后台管理员使用独立随机强密码，监控使用独立令牌。
- [x] `npm run links:check` 已执行，无盗版、非 HTTPS、私网或确认失效地址；126 条入口中 83 条健康，43 条受地区网络、反爬或 5xx 影响的暂无法确认项保留人工复核。
- [x] 已对 132 位目录人物、109 条图鉴索引、125 部正式作品、126 条有效正版/馆藏链接及图鉴关联关系执行分层复核；第 1～3 卷核心人物及鲁平、方木已补齐权威来源，73 条已提取图鉴推荐全部显示正式关联目标。
- [x] 小程序空态、断网、接口超时、无权限和内容已删除场景已通过动态失败/重试检查；正式微信真机体验仍归入 P0 真机验收。
- [x] 社区动态支持跨作品短评/长评筛选、分页和剧透保护；作品下架或作者停用后相关评价不再公开，API 总数与结果同步。
- [x] 运营上岗演练已提供四场景步骤、仓库外记录模板、90 天有效期、源码/运行手册绑定回执和自动化提审门禁。
- [ ] 清单中命名的真实运营人员已完成审核、举报结案、用户处置和紧急下架演练，且 `operations_drill` 门禁为 `READY`。

## P2：发布后一周内观察

- [ ] 每日检查错误率、P95 延迟、登录成功率、审核积压和备份状态。
- [ ] 收集首批用户反馈，建立缺陷优先级和修复时限。
- [ ] 根据真实访问量调整连接池和限流阈值。
- [ ] 对高频搜索无结果项补充别名和作品资料。
